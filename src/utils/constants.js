// ============================================================
// constants.js
// 全局常量定义
// ============================================================
// 包含:
// - 图形相关(节点半径、视图区域)
// - 缩放相关(最小/最大比例、缩放因子)
// - 动画相关(自动播放间隔)
// - 颜色相关(模式配色)
// - 范围相关(年份范围)
// ============================================================

// ===================== 图形 =====================

/**
 * 节点半径(像素)
 * 决定节点圆圈的大小
 */
export const NODE_RADIUS = 28;

/**
 * SVG视图区域
 * 定义SVG内部坐标系的宽高
 * 格式: "minX minY width height"
 *
 * origin在左上角
 * 宽度1200, 高度640
 */
export const VIEW_BOX = "0 0 1200 640";

// ===================== 缩放 =====================

/**
 * 最小缩放比例
 * 50% (只能缩小到一半)
 */
export const MIN_SCALE = 0.5;

/**
 * 最大缩放比例
 * 150% (只能放大到1.5倍)
 */
export const MAX_SCALE = 1.5;

/**
 * 滚轮缩放因子
 * 每格缩放5%
 * deltaY > 0 -> 缩小: scale / 1.05
 * deltaY < 0 -> 放大: scale * 1.05
 */
export const ZOOM_FACTOR = 1.05;

/**
 * 按钮放大因子
 * 点击放大按钮时 scale * 1.2 (20%)
 */
export const SCALE_BUTTON_FACTOR_IN = 1.2;

/**
 * 按钮缩小因子
 * 点击缩小按钮时 scale * 0.8 (20%)
 */
export const SCALE_BUTTON_FACTOR_OUT = 0.8;

// ===================== 动画 =====================

/**
 * 自动播放间隔(毫秒)
 * 步骤自动播放时,每步之间间隔900ms
 */
export const AUTO_PLAY_INTERVAL = 900;

// ===================== 颜色 =====================

/**
 * 模式配色
 * 返回RGB字符串,用于CSS颜色
 *
 * @param {string} mode - 模式 "bfs" | "dfs" | "explore"
 * @returns {string} "R,G,B" 格式
 *
 * 示例:
 *   modeColor("bfs")   -> "74,144,217" 蓝色
 *   modeColor("dfs")    -> "46,204,113" 绿色
 *   modeColor("explore") -> "200,160,69" 古铜色
 */
export const modeColor = (mode) =>
  mode === "bfs" ? "74,144,217" : mode === "dfs" ? "46,204,113" : "200,160,69";

// ===================== 范围 =====================

/**
 * 年份范围
 * 历史节点的年份在此范围内
 * 公元前8000年 ~ 公元2026年
 */
export const YEAR_RANGE = { min: -8000, max: 2026 };
